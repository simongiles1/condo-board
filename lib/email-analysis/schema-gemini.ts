/**
 * Gemini response schema for email extraction is intentionally omitted.
 * The full 7-domain schema exceeds Gemini responseSchema complexity limits;
 * generation uses JSON mode + validateEmailExtraction() instead (same pattern as minutes v2).
 */

export const EMAIL_EXTRACTION_USES_JSON_MODE = true;
